# 参与贡献

感谢你愿意改进掌间星云。Bug 修复、性能优化、手势体验和新粒子形态都很欢迎。

## 开始之前

1. Fork 仓库并从 `main` 创建功能分支。
2. 运行 `python3 scripts/fetch_mediapipe.py` 准备本地识别资源。
3. 使用 `python3 server.py` 启动项目。
4. 在最新版 Chrome 中同时测试摄像头模式和鼠标降级模式。

## 修改原则

- 保持项目无构建步骤、可直接由静态服务器运行。
- 不上传或持久化摄像头画面。
- 修改手势阈值时，同时验证快动作不改变缩放、慢动作可双向缩放。
- 修改 WebGL 逻辑时，检查三种形态、三种配色和不同粒子密度。
- 保持桌面端与移动端无横向溢出。
- 第三方依赖必须固定版本，并保留许可证信息。

## 提交建议

- 使用简洁的动词式提交信息，例如 `Improve slow-close zoom stability`。
- 一个提交尽量只解决一类问题。
- Pull Request 中说明修改动机、测试方式和可见变化；涉及视觉变化时附截图或录屏。

## 报告问题

提交 Issue 时请提供：

- 浏览器与操作系统版本；
- 是否使用摄像头、鼠标或触屏；
- 可复现步骤和预期结果；
- 控制台错误（如有）；
- 不要上传包含个人环境或敏感信息的摄像头画面。

